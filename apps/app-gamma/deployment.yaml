apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-gamma
  labels:
    app: app-gamma
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app-gamma
  template:
    metadata:
      labels:
        app: app-gamma
    spec:
      containers:
      - name: app-gamma
        image: nginx:latest
        ports:
        - containerPort: 80
        volumeMounts:
        - name: html
          mountPath: /usr/share/nginx/html
      volumes:
      - name: html
        configMap:
          name: app-gamma-html
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-gamma-html
data:
  index.html: |
    <h1>Hello from App gamma</h1>
